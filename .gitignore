# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env
.env.staging
.env.demo
.env.production
.env.local
.env.*.local

# Firebase credentials (NEVER commit these!)
firebase-service-account.json
config/firebase-service-account.json
**/firebase-service-account.json

# Editor/IDE
# .idea/
# .vscode/

# =================================================
# VPS Deployment (added for multi-env deployment)
# =================================================

# Backup files
*.backup
*.bak
backups/
*.sql.gz

# Old/temporary files
*.old
*.tmp
*.temp

# Build artifacts
build/
dist/
tmp/

# Log files (local)
*.log
logs/*.log

# Certificates and keys
*.pem
*.key
*.crt
!nginx/*.conf

# Docker volumes data (local)
postgres-data/
redis-data/
pgadmin-data/

# Upload directories (should be empty in repo)
uploads/avatars/*
uploads/company/*
uploads/documents/*
!uploads/avatars/.gitkeep
!uploads/company/.gitkeep
!uploads/documents/.gitkeep

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
